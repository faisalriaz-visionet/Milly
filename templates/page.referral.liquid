<main class="content referral" role="main">
  <div class="container page page--no-sidebar">
    <div class="page__content">
      {% if page.content.size > 0 %}
        <div class="rte">
          {{ page.content }}
        </div>
      {% endif %}
    </div>
  </div>
</main>
